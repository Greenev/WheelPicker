<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="itemTextSize"
            type="float" />
    </data>

    <LinearLayout xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/ll_wp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:orientation="vertical">

        <View
            android:id="@+id/wp_top_border"
            android:layout_width="@dimen/wp_divider_width"
            android:layout_height="@dimen/wp_divider_height"
            android:layout_gravity="center"
            android:background="@android:color/black" />

        <TextView
            android:id="@+id/wp_item"
            android:layout_width="match_parent"
            android:layout_height="@dimen/wp_item_height"
            android:layout_gravity="center"
            android:background="@android:color/transparent"
            android:gravity="center"
            android:maxLines="1"
            android:singleLine="true"
            android:textSize="@{itemTextSize}"
            tools:text="@string/view_name" />

        <View
            android:id="@+id/wp_bottom_border"
            android:layout_width="@dimen/wp_divider_width"
            android:layout_height="@dimen/wp_divider_height"
            android:layout_gravity="center"
            android:background="@android:color/black" />
    </LinearLayout>
</layout>